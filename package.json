{
  "name": "linkcito",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "prisma": {
    "schema": "packages/db/prisma/schema.prisma"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "dev:bot": "turbo run dev --scope=bot",
    "dev": "turbo run dev --parallel",
    "generate": "turbo run generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@7.14.0",
  "devDependencies": {
    "turbo": "^1.6.3"
  },
  "engines": {
    "node": ">=16.6.0"
  }
}
